ci:
  collect:
    url: ['http://localhost:3000/dibbs-site']
  assert:
    assertions:
      categories:accessibility: [error, { minScore: 1 }]
  upload:
    # Add upload options here if needed, e.g., for temporary storage or specific storage providers
  server:
    # Add any additional server options here if required
  wizard:
    # Wizard options here if needed
